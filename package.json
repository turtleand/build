{
  "name": "build",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "node scripts/snippet-sync.mjs --watch & astro dev",
    "generate:llms": "node scripts/generate-llms.mjs",
    "build": "node scripts/snippet-sync.mjs && node scripts/generate-llms.mjs && astro build",
    "preview": "astro preview",
    "perf:audit": "node scripts/perf-audit.mjs",
    "astro": "astro",
    "test": "playwright test",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
